# Ubuntu initramfs-tools configuration for the Samsung Galaxy Book4 Edge.
# The workflow translates MODULES and FILES from mkinitcpio-book4.conf.

MODULES=most
BUSYBOX=auto
COMPRESS=zstd
KEYMAP=n
FRAMEBUFFER=y
BOOT=local